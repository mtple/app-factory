import { buildFarcasterManifest } from "@app-factory/farcaster";
import { palettePotionConfig } from "../app/config";

export const palettePotionManifest = buildFarcasterManifest(palettePotionConfig);
